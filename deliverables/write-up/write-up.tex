\documentclass[11pt]{article}

\usepackage[left=0.95in,right=0.95in,top=0.8in,bottom=0.8in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[table]{xcolor}
\usepackage{graphicx}
\usepackage{float}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{array}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{caption}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{tikz}

\definecolor{ink}{HTML}{1A1A1A}
\definecolor{rulegray}{HTML}{D8D8D8}
\definecolor{linkblue}{HTML}{1B4F9C}
\definecolor{shotborder}{HTML}{BFBFBF}
\color{ink}

\hypersetup{colorlinks=true, urlcolor=linkblue, linkcolor=linkblue}

\titleformat{\section}{\large\bfseries\color{ink}}{\thesection.}{0.55em}{}
\titlespacing*{\section}{0pt}{1.0em}{0.4em}
\titleformat{\subsection}{\normalsize\bfseries\color{ink}}{\thesubsection}{0.55em}{}
\titlespacing*{\subsection}{0pt}{0.7em}{0.15em}

\captionsetup{font=small, labelfont={bf}, labelsep=period, skip=5pt}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0.45em}
\linespread{1.06}

\renewcommand{\arraystretch}{1.35}
\arrayrulecolor{rulegray}

\setlength{\textfloatsep}{0.5em}
\setlength{\intextsep}{0.5em}

\newsavebox{\shotbox}
\newlength{\shotwd}
\newlength{\shotht}
\newcommand{\codeshot}[2][\textwidth]{%
  \sbox{\shotbox}{\includegraphics[width=#1]{#2}}%
  \settowidth{\shotwd}{\usebox{\shotbox}}%
  \settoheight{\shotht}{\usebox{\shotbox}}%
  \begin{tikzpicture}
    \begin{scope}
      \clip[rounded corners=4pt] (0,0) rectangle (\shotwd,\shotht);
      \node[anchor=south west,inner sep=0pt] at (0,0) {\usebox{\shotbox}};
    \end{scope}
    \draw[rounded corners=4pt,draw=shotborder,line width=0.5pt] (0,0) rectangle (\shotwd,\shotht);
  \end{tikzpicture}%
}

\usetikzlibrary{positioning,arrows.meta,calc}

\graphicspath{{images/}}

\begin{document}

\begin{center}
{\LARGE\bfseries\color{ink} Project Write-Up}\\[0.15em]
{\normalsize\bfseries\color{ink} CMPT 310, Group 1}\\[0.15em]
\href{https://github.com/praneetkb/2048-Master}{github.com/praneetkb/2048-Master}
\end{center}
\vspace{1em}

\section{System Explanation}

\subsection{What our system does}

2048 is a sliding puzzle on a 4×4 grid. Every move pushes all the tiles in one direction, and when two tiles with the same number collide, they merge into one tile, adding the merged value to the score. After every move, the game adds a tile valued 2 or 4 on a random empty cell. The game ends when the board is full and no movement of tiles can happen.

Our system plays the game by itself: it takes the current board, returns one legal move, and repeats until no move changes the board. We wrote the engine ourselves, including tile movement and merging, score and rewards, the random 2 (90\%) or 4 (10\%) tile spawn, and the game-over check.

\subsection{What AI methods we use}

We use two methods from the course. The first is reinforcement learning: TD(0) over an N-tuple network, which learns a value function $V(s)$ from self-play. The second is Expectimax, the chance-node version of the adversarial search we covered in class, which uses that value function to look ahead through the random spawns.

\subsection{The pipeline}

We train the value function separately and save it to a file. During a game the agent only reads that file, so the two halves are independent and nothing is learned while playing.

\begin{figure}[H]
\centering
\vspace{0.5em}
\hyphenpenalty=10000\exhyphenpenalty=10000\relax
\resizebox{0.80\textwidth}{!}{%
\begin{tikzpicture}[
  font=\scriptsize,
  node distance=2.6mm and 7mm,
  proc/.style={rectangle, draw, fill=black!2, align=center,
    text width=53mm, minimum height=5mm, inner sep=2.5pt},
  io/.style={proc, rounded corners=3pt, fill=black!6},
  ckpt/.style={rectangle, rounded corners=3pt, draw, dashed, align=center,
    text width=19mm, minimum height=5mm, inner sep=2.5pt},
  lane/.style={font=\small\bfseries},
  arr/.style={-{Latex[length=1.8mm]}, semithick}
]
  \node[io]   (obs)   {Self-play board: 4×4 grid of tile values};
  \node[proc] (enc)   [below=of obs] {Preprocessing: each cell becomes its tile rank
    $\log_2$, so four cells form a base-16 index};
  \node[proc] (net)   [below=of enc] {N-tuple network (the model): 17 four-cell patterns,
    one 65,536-entry table each, read over 8 symmetries};
  \node[proc] (td)    [below=of net] {TD(0) update: next reward plus $V$ of the next
    afterstate, spread over 136 entries};

  \node[lane] (lgt) [above=3mm of obs] {Offline training, run once};

  \node[io]   (board) [right=36mm of obs] {Current 4×4 board during a game};
  \node[proc] (search)[below=of board] {Expectimax: max node over legal moves,
    chance node over spawns (2: 0.9, 4: 0.1)};
  \node[proc] (leaf)  [below=of search] {Leaves are afterstates scored by $V(s)$;
    depth 2, deepening to 3 when 2 cells are empty};
  \node[io]   (out)   [below=of leaf] {Output: one of the four moves};

  \node[lane] (lgp) [above=3mm of board] {Online play, every move};

  \node[ckpt] (cp) at ($(td.east)!0.5!(leaf.west)$)
    {saved value function \texttt{.npz}};

  \draw[arr] (obs) -- (enc);
  \draw[arr] (enc) -- (net);
  \draw[arr] (net) -- (td);
  \draw[arr] (board) -- (search);
  \draw[arr] (search) -- (leaf);
  \draw[arr] (leaf) -- (out);
  \draw[arr, dashed] ([yshift=-3mm]td.east) -| (cp.south);
  \draw[arr, dashed] (cp.north) |- (leaf.west);
  \draw[arr] (td.west) -- ++(-4mm,0) |- (obs.west)
    node[pos=0.25, left, align=right, font=\scriptsize] {greedy\\self-play};
\end{tikzpicture}
}
\captionsetup{skip=10pt}
\caption{The two pipelines. Training writes the value function once, and play reads it on every move.}
\end{figure}

Both halves meet on afterstates, the boards you get after a move but before the spawn. A move in 2048 is deterministic and only the spawn is random, so the afterstate is the last position we can evaluate before luck enters. While training, we correct the value of the afterstate we just left using the reward of the next move plus the value of the next afterstate, or zero if the game ended there. The search stops at that same point, so the learned $V$ goes straight into Expectimax with nothing to convert. The eight board symmetries are what make training affordable, since every position we see updates all 136 shared entries at once.

\subsection{Results}

We recorded the average score of every block of 1,000 self-play episodes, then played the finished agent against two baselines for 30 games each in the same environment.

\begin{figure}[H]
\centering
\includegraphics[width=0.50\textwidth]{training_curve.png}
\caption{Average score per 1,000 training episodes. Self-play score rises from 8,384 to 24,382.}
\end{figure}

\begin{table}[H]
\centering
\setlength{\tabcolsep}{4pt}
\small
\begin{tabular}{lccccccc}
\toprule
\textbf{Agent} & \textbf{Games} & \textbf{Avg} & \textbf{Median} & \textbf{Best} & \textbf{Best tile} & \textbf{Reached 2048} & \textbf{ms/move} \\
\midrule
Random & 30 & 1,178 & 990 & 3,076 & 256 & 0/30 & 0.0 \\
Expectimax (heuristic) & 30 & 14,724 & 15,698 & 27,308 & 2,048 & 5/30 & 8.8 \\
Expectimax + RL & 30 & 62,244 & 66,646 & 120,744 & 8,192 & 30/30 & 12.9 \\
\bottomrule
\end{tabular}
\caption{The same search with a learned value function instead of a hand-written one, on the final code.}
\end{table}

Swapping the hand-written evaluation for the learned one, inside the exact same search, multiplies the average score by 4.2 and takes the agent from 5 wins in 30 games to 30, at about 4 extra milliseconds per move.

\subsection{Limitations and what we did about them}

Three things still limit the agent. Learning starts slowly, because every TD update is divided among all 136 entries it touches, so the real step per weight is much smaller than $\alpha$ suggests; our first rate of 0.01 only reached about 15,500 after 10,000 episodes, and we settled on 0.05 after measuring both. Fixed depth 2 is also weakest exactly where games are lost, since on a nearly full board one bad spawn can leave no good move and a two-ply search sees only one spawn ahead. Two or fewer empty cells make the chance node small, so we deepen to 3 precisely there, which raises the cost from roughly 8 to 13 milliseconds per move and stays fast enough to watch. Finally, the value function knows nothing about board structure beyond its 17 local patterns, which is why we still need the search: playing greedily with no lookahead, the same network averages about 25,200 and wins half its games, against 62,244 and 30 out of 30 inside Expectimax. We kept the heuristic baseline for the same reason, to separate what learning contributes from what search does.

\clearpage
\section{Feature Table}

The table below summarizes the major components implemented for the 2048 environment, AI agents, reinforcement learning pipeline and user interface.

\begingroup
\fontsize{6.9}{7.5}\selectfont
\setlength{\tabcolsep}{2.2pt}
\renewcommand{\arraystretch}{0.92}
\begin{longtable}{@{}>{\raggedright\arraybackslash}p{0.15\textwidth}>{\centering\arraybackslash}p{0.06\textwidth}>{\centering\arraybackslash}p{0.105\textwidth}>{\centering\arraybackslash}p{0.09\textwidth}>{\centering\arraybackslash}p{0.11\textwidth}>{\raggedright\arraybackslash}p{0.375\textwidth}@{}}
\toprule
\multicolumn{1}{c}{\textbf{Description}} & \multicolumn{1}{c}{\textbf{Platform}} & \multicolumn{1}{c}{\textbf{Completeness}} & \multicolumn{1}{c}{\textbf{Code}} & \multicolumn{1}{c}{\textbf{Author(s)}} & \multicolumn{1}{c}{\textbf{Notes}} \\
\midrule
\endfirsthead
\toprule
\multicolumn{1}{c}{\textbf{Description}} & \multicolumn{1}{c}{\textbf{Platform}} & \multicolumn{1}{c}{\textbf{Completeness}} & \multicolumn{1}{c}{\textbf{Code}} & \multicolumn{1}{c}{\textbf{Author(s)}} & \multicolumn{1}{c}{\textbf{Notes}} \\
\midrule
\endhead
\bottomrule
\endfoot
4x4 board \& tile representation & Local & 5 & Python (NumPy) & João & Represents the board, values for each tile and empty cells. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Tile movement and merging & Local & 5 & Python (NumPy) & Praneet & Implements up, down, left, and right movement and tile merging behavior when tiles collide. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Score, rewards, and game termination & Local & 5 & Python & Ryan & Handles rewards, score updates, legal-move checks, and game-over conditions. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Random tile spawning & Local & 5 & Python & Jayden & Randomly spawns a 2 or 4 tile on empty cells after a legal move using the probability distribution. Missing test cases. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Random agent & Local & 5 & Python & Jayden & Selects legal moves randomly and serves as a baseline for evaluating other stronger agents. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Heuristic Expectimax agent & Local & 5 & Python & João, Praneet, Ryan & Implements max and chance nodes, expected-value calculation, search depth, termination, and a hand-designed board evaluation function. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
N-tuple network value function & Local & 5 & Python (NumPy) & João & Learns board-state values using multiple tuple patterns, lookup tables, and board symmetries. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
TD learning training pipeline & Local & 4 & Python (NumPy) & Praneet & Trains the N-tuple value function through self-play using Temporal Difference learning and periodic checkpoints. Missing test cases. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
RL and Expectimax integration & Local & 5 & Python & Ryan & Uses the learned N-tuple value function as the evaluation function inside the Expectimax search. Missing some test cases. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Agent evaluation and performance comparison & Local & 5 & Python & Jayden & Compares Random, heuristic Expectimax, and Expectimax + RL. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
4x4 board and tile renderer & Local & 5 & Python (Pygame) & João & Displays the game board and tile values in the GUI. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Game loop and tile animations & Local & 5 & Python (Pygame) & Jayden & Connects and controls the overall gameplay and implemented tile animations for moving and merging. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Main menu and game header & Local & 5 & Python (Pygame) & Praneet, Ryan & Allows user to select between 3 agents to watch play and a comparison mode in the main menu. Displays the game header: title, score, best score, restart. \\ \specialrule{0.25pt}{2.75pt}{2.75pt}
Automated testing & Local & 3 & Python (Pytest) & João, Ryan, Praneet & Tests cover core game mechanics, agents, N-tuple network behavior, and UI components. More tests could have been written. \\
\end{longtable}
\endgroup

\section{External Tools \& Libraries}

\begingroup
\footnotesize
Our project was implemented in Python and we used some external libraries to support numerical computation, reinforcement learning, visualization, testing, and the graphical user interface.

\begin{itemize}[leftmargin=1.4em, itemsep=0.05em, topsep=0.1em]
  \item Matplotlib: Used to generate the TD-learning training-progress graph showing average score across training episodes.
  \item Pytest: Used for automated testing of the game environment, agents, N-tuple network, and UI components.
  \item NumPy: Used for efficient representation and manipulation of the 4x4 game board, movement operations, and N-tuple network computations.
  \item Pygame: Used to implement the graphical user interface, including the main menu, game board, tile rendering, score display, and tile animations.
\end{itemize}

No external dataset or API was required. We also did not reuse an external 2048 implementation for the core game environment or AI agents. The project architecture, game environment and mechanics, Expectimax implementation, N-tuple network, TD-learning pipeline, and UI were developed by our group.

Our project does not use an external dataset. Instead, the reinforcement learning training data is generated automatically through self-play.

The project can be run locally after installing the dependencies listed in \texttt{requirements.txt}.
\endgroup

\end{document}
